2025/10/14
    Выполнена задача "task2". Необходимо каждую строку (если подразумевается, что она будет использована как 
    строка) заканчивать нуль-терминатором. Еще нужно проверять массивы на переполнение, и , самое важное, 
    необходимо обнулять массивы при определении, иначе внутри будет всякий мусор. В качестве улучшения можно
    использовать динамическое выделение памяти.
    Начата задача "task3". В качестве улучшения можно добавить функцию, объединяющую два массива в один, и, 
    если возможно, хотелось бы сделать подобие функции map() в python (почленно к каждому элементу массива
    применять некоторую функцию).
2025/10/20
    Успешно завершена задача "task3". Необходимо после каждого вызова scanf() очищать буфер ввода
    while(getchar() != '\n'). Кроме того при выделении памяти необходимо каждый раз проверять, получилось или 
    нет. Если для какого либо объекта память выделялась динамически, то sizeof() к ним не применим, поэтому
    в качестве аргументов любых функций, меняющих размер массива, необходимо доболнительно присылать указатель 
    на переменную, в которой содержится размер массива.
